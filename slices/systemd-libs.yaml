package: systemd-libs

essential:
  - systemd-libs_copyright

slices:
  libs:
    essential:
      - glibc_libs
      - libcap_libs
    contents:
      /usr/lib*/libsystemd.so.0*:
      /usr/lib*/libudev.so.1*:

  copyright:
    contents:
      /usr/share/licenses/systemd/LICENSE.LGPL2.1: